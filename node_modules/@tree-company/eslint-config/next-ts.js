module.exports = {
  // Setup parser and options
  parser: "@typescript-eslint/parser",
  parserOptions: {
    ecmaVersion: 2020,
    sourceType: "module",
    ecmaFeatures: {
      jsx: true,
    },
  },
  env: {
    es6: true,
    node: true,
    browser: true,
  },
  // Ignore node_modules and next generated files
  ignorePatterns: ["/node_modules/**", "/.next/**", "/out/**"],
  // Config plugins and rules
  plugins: ["react", "prettier"],
  extends: [
    "plugin:@typescript-eslint/recommended",
    "next",
    "plugin:prettier/recommended",
  ],
  rules: {
    // We don't define the return type for React components
    "@typescript-eslint/explicit-module-boundary-types": "off",
    // We don't always want to use next/image
    "@next/next/no-img-element": "off",
    // We always trust the pages we link to
    "react/jsx-no-target-blank": "off",
  },
  settings: {
    react: {
      version: "detect",
    },
  },
};
